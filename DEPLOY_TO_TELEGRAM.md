# 🚀 Как запустить GRAVITEXT в Telegram

Чтобы игра заработала внутри Telegram, её нужно разместить на хостинге с поддержкой HTTPS (это требование Telegram). Самый простой и бесплатный способ — **GitHub Pages**.

## Шаг 1: Подготовка файлов

Убедитесь, что у вас есть все файлы игры в одной папке:
- `index.html`
- `styles.css`
- `js/` (папка со скриптами)
- `assets/` (если есть картинки/звуки)

## Шаг 2: Загрузка на GitHub

1. Зарегистрируйтесь на [github.com](https://github.com), если нет аккаунта.
2. Создайте новый репозиторий (кнопка **New** или **Create repository**).
   - Назовите его, например, `gravitext-game`.
   - Обязательно выберите **Public** (пунктир).
   - Нажмите **Create repository**.
3. Загрузите файлы:
   - Нажмите **uploading an existing file**.
   - Перетяните все файлы и папки игры в окно браузера.
   - Нажмите зеленую кнопку **Commit changes**.

## Шаг 3: Включение GitHub Pages

1. В репозитории перейдите в **Settings** (Настройки).
2. В меню слева выберите **Pages**.
3. В разделе **Build and deployment** -> **Branch** выберите `main` (или `master`) и папку `/(root)`.
4. Нажмите **Save**.
5. Подождите 1-2 минуты. Обновите страницу.
6. Сверху появится ссылка вида: `https://ваш-ник.github.io/gravitext-game/`. **Скопируйте её**.

## Шаг 4: Создание бота в Telegram

1. Откройте Telegram и найдите бота **@BotFather**.
2. Напишите команду `/newbot`.
3. Придумайте имя боту (например, `Gravitext Game`).
4. Придумайте юзернейм (должен заканчиваться на `bot`, например, `GravitextGame2024Bot`).
5. Вы получите **API Token** — сохраните его (он пригодится, если будете делать серверную часть, но для игры пока не обязателен).

## Шаг 5: Настройка Web App

1. В **@BotFather** напишите команду `/mybots`.
2. Выберите вашего бота.
3. Нажмите **Bot Settings** -> **Menu Button** (или **Type** -> **Web App**).
4. Нажмите **Configure Menu Button**.
5. Отправьте боту ссылку, которую вы скопировали на Шаге 3 (с GitHub Pages).
6. Введите название кнопки (например, `Играть`).

## Готово! 🎉

Теперь зайдите в своего бота. Слева от поля ввода появится кнопка (или меню), нажав на которую откроется ваша игра прямо внутри Telegram!

### 💡 Полезные советы:

- **Тестирование:** Если вы хотите тестировать локально без загрузки на GitHub, используйте [ngrok](https://ngrok.com/).
  - Установите ngrok.
  - Запустите локальный сервер (например, через Visual Studio Code "Live Server").
  - Выполните команду `ngrok http 5500` (если порт 5500).
  - Скопируйте HTTPS ссылку и отправьте BotFather (через `/newapp` или настройку кнопки).

- **Обновления:** Чтобы обновить игру, просто загрузите новые файлы в репозиторий GitHub. Изменения появятся автоматически через пару минут.
